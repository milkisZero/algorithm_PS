#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(0), cin.tie(NULL), cout.tie(NULL)
using namespace std;
int main(){
    fastio;
    string s; cin >> s;
    cout << s.length();
    return 0;
}